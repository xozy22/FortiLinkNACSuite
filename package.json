{
  "name": "fortilink-nac-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Management UI for FortiLink NAC dynamic port policies on FortiGate",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w web",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "npm run preview -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
